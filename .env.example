# Book Tracker Environment Configuration
# Copy this file to .env and update with your production values

# Server Configuration
NODE_ENV=production
PORT=3000

# Admin Credentials
# IMPORTANT: Change these values in production! Use strong passwords.
# Note: Avoid special characters like $ @ ! ( ) etc - they can cause parsing issues
# Use alphanumeric characters, hyphens, and underscores only
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password-here

# Database Configuration (Optional)
# If DATABASE_URL is not set, the app will use file-based storage (data.json)
# For PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Example for Render.com PostgreSQL:
# DATABASE_URL=postgresql://user:password@host:5432/database_name
